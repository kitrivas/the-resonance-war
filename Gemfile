source "https://rubygems.org"

# GitHub Pages compatible build. If you deploy via GitHub Pages' built-in
# Jekyll, use the github-pages gem; if you build with Actions, plain jekyll.
gem "jekyll", "~> 4.3"

group :jekyll_plugins do
end
