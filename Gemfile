source "https://rubygems.org"

# GitHub Pages build environment. This matches what the Actions build
# (actions/jekyll-build-pages) uses, so local `jekyll serve` and CI agree.
gem "github-pages", group: :jekyll_plugins
gem "webrick" # required by Jekyll on Ruby 3+
